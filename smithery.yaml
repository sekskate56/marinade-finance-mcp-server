runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
env:
  ENVIRONMENT: "MAINNET"
  USE_STREAMABLE_HTTP: "true"
  PORT: "3000"
  HOST: "127.0.0.1"